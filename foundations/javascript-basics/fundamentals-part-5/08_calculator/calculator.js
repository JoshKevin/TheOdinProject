const add = function(a, b) {
    return a + b;
};

const subtract = function(a, b) {
	return a - b
};

const sum = function(array) {
	let total = array.reduce((a, b) => {
        return a + b;
    }, 0);
};

console.log(sum(3, 5, 6, 10))
const multiply = function() {

};

const power = function() {
	
};

const factorial = function() {
	
};

